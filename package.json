{
  "name": "mprest-workspace",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "mprest-map-core",
    "mprest-map-cesium",
    "mprest-app"
  ],
  "scripts": {
    "dev": "npm run dev --workspace mprest-app",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "npm test --workspaces"
  }
}
